Use the mcp-playwright server to navigate to  http://localhost:8080
Do not simulate tests statistically or baypass MCP server, ALL interactions must be routed throught MCP

On the login page, execute the following two scenarios:

Scenario 1: Valid Login

The login data are taken from the test data, they are in the readmee file.
After scenarios are validated:
Close the browser.

Scenario 2: Valid Login
After logging in, check that the player data in the player profile matches the data in the database, use the API to download the player data

Once the MCP server finished the code gen, use the code generated under temp_codegen and transpile it to Javascript Playwright Framework using the code generated as reference.

Once you have the code gen thru the MCP server, and use the code generated.

Generate the best framework structure using Page Object Model, saving data if necessary, save the credentials in test ata file

Update readme.md file to explain how to run the tests, and the structure of the project.

Create a gitignore file to avoid committing the node_modules folder and the cypress.env.json file.

Create a CICD pipeline using Jenkins  to run the tests.